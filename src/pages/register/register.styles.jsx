import styled from 'styled-components';
import { whiteColor } from '../../app.utils';

export const RegisterContainer = styled.div`
    height: 100vh;
    background-color: ${whiteColor};
    display: flex;
    align-items: center;
    justify-content: center;
`;